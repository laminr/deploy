{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Command creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('command') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
